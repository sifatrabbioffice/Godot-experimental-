name: 🧪 Build PURE Native Godot 4.7 iOS Editor

on:
  workflow_dispatch:

jobs:
  build-godot-native-ios:
    runs-on: macos-15
    steps:
      - name: Checkout Godot Source Code
        uses: actions/checkout@v4
        with:
          repository: godotengine/godot
          ref: '4.7'
          path: godot-source

      - name: Setup Python and SCons
        run: |
          python3 -m pip install scons --break-system-packages

      - name: Compile Real Godot Engine for iOS
        run: |
          cd godot-source
          # iPhone 15 Pro Max (A17 Pro) এর জন্য নেটিভ স্ট্যাটিক লাইব্রেরি কম্পাইল করা হচ্ছে
          scons platform=ios target=editor arch=arm64 precision=single -j$(sysctl -n hw.ncpu)

      - name: Create iOS App Bundle Structure
        run: |
          mkdir -p build_stage/GodotEditor.app

      - name: Link Static Library into Real iOS Executable
        run: |
          # বিল্ড ফোল্ডারে কি কি ফাইল তৈরি হয়েছে তা দেখার জন্য (Debugging)
          ls -la godot-source/bin/
          
          # অ্যাপলের নেটিভ লিঙ্কার (clang++) ব্যবহার করে .a ফাইলটিকে আইফোনের ফ্রেমওয়ার্কের সাথে জোড়া হচ্ছে
          xcrun --sdk iphoneos clang++ -target arm64-apple-ios17.0 \
            -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
            godot-source/bin/*.a \
            -framework UIKit -framework Foundation -framework Metal -framework MetalKit \
            -framework CoreGraphics -framework QuartzCore -framework AudioToolbox \
            -framework AVFoundation -framework CoreAudio -framework CoreMIDI \
            -framework CoreMotion -framework GameController -framework Security \
            -framework CoreVideo -framework IOKit \
            -lc++ -lz \
            -o build_stage/GodotEditor.app/GodotEditor

          # এক্সিকিউটেবল পারমিশন দেওয়া হচ্ছে
          chmod +x build_stage/GodotEditor.app/GodotEditor

      - name: Generate High-Performance Info.plist
        run: |
          cat << 'EOF' > build_stage/GodotEditor.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>GodotEditor</string>
              <key>CFBundleIdentifier</key>
              <string>org.godotengine.officialnativeeditor</string>
              <key>CFBundleName</key>
              <string>Godot</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>4.7</string>
              <key>CFBundleVersion</key>
              <string>1.0</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>MinimumOSVersion</key>
              <string>17.0</string>
              <key>UIFileSharingEnabled</key>
              <true/>
              <key>LSSupportsOpeningDocumentsInPlace</key>
              <true/>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>
              <key>UIRequiredDeviceCapabilities</key>
              <array>
                  <string>arm64</string>
                  <string>metal</string>
              </array>
          </dict>
          </plist>
          EOF

      - name: Sign and Package Absolute Real IPA
        run: |
          mkdir -p Payload
          mv build_stage/GodotEditor.app Payload/
          
          # অ্যাড-হক ডিজিটাল সিগনেচার করা হচ্ছে
          codesign --force --deep --sign - Payload/GodotEditor.app
          
          # ফাইনাল আইপিএ প্যাকেজিং
          zip -r Godot4.7_Native_iPhone15ProMax.ipa Payload/

      - name: Upload Complete Engine IPA
        uses: actions/upload-artifact@v4
        with:
          name: godot-4.7-pure-native-ipa
          path: Godot4.7_Native_iPhone15ProMax.ipa
